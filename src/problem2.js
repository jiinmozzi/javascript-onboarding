function problem2(cryptogram) {
  var answer;
  
  class Node {
    constructor(data, next = null){
      this.data = data;
      this.next = next
    }
  }
  
  class LinkedList {
    constructor(){
      this.head = null;
    }
    insertFirst(){}
    insertLast(){}
  }

  
  const generateLinkedList = () => {}

  // remove continuously duplicate alphabets from linkedList 
  const decode  = () => {}

  // make a whole word from filtered linked list
  const getAnswer = () => {} 

  return answer;
}

module.exports = problem2;
